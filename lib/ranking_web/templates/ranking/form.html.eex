<%= form_for @conn, @action, fn f -> %>

  <%= label f, :name %>
  <%= text_input f, :name %>

  <%= label f, :score %>
  <%= text_input f, :score %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
